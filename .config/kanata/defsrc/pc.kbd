;; Source key capture (35 keys).
;;
;; Flat order (matches every deflayer token-for-token):
;;   top num row:    1 2 3 4 | 8 9 0 - =
;;   top alpha:      q w e r | i o p [ ]
;;   home alpha:     caps a s d f | k l ; ' ret
;;   bottom alpha:   x c | , .
;;   misc:           tab prtsc `
(defsrc
  1    2    3    4    8    9    0    -    =
  q    w    e    r    i    o    p    [    ]
  caps a    s    d    f    k    l    ;    '    ret
  x    c    ,    .
  tab
  prtsc
  `
)
