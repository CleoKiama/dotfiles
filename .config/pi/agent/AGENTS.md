# Global Preferences

## Shell Aliases
If you need aliases, run: `shopt -s expand_aliases && source ~/.zshrc`

## Commit Style
Use conventional commits with gitmoji. Run the `/skill:git-commit` skill for detailed guidance.

## Defaults
- Use read, bash, edit, write tools as needed
- Prefer concise, actionable responses
- Show file paths clearly when working with files
