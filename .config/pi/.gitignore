# Secrets — never commit
agent/auth.json
agent/auth.json.*

# Sessions — auto-generated, not config
agent/sessions/
agent/mcp-cache/
agent/mcp-oauth/
agent/mcp-cache.json
.pi/

# Auto-installed packages via pi install
agent/git/
agent/npm/

# Local state — machine-specific trust decisions
agent/trust.json

# Temp / runtime
*.tmp


agent/models-store.json
